
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link href="{{constant('php\\Bootstrap::APP_URL')}}css/work-page.css" rel="stylesheet">
    <link rel="stylesheet" href="{{constant('php\\Bootstrap::APP_URL')}}css/stylesheet.css">
    <link rel="stylesheet" href="{{constant('php\\Bootstrap::APP_URL')}}css/work-page.css">
    <link rel="stylesheet" href="{{constant('php\\Bootstrap::APP_URL')}}css/responsive.css">
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">
  </head>
  <body>

  {% include 'header.html.twig' %}

   {% include 'category_menu.html.twig' %}
    <div id="work-main">

      <div id="cart">

        <p>保存した求人リスト</p>
        <p><a href="{{constant('php\\Bootstrap::ENTRY_URL')}}list.php">案件一覧へ戻る</a></p>
        <p><a href="{{constant('php\\Bootstrap::ENTRY_URL')}}cart.php?ukeou=4">エントリー</a></p>
      {% if result | length > 0 %}<br><p>{{ result }}</p>{% endif %}
        <p>求人数：{{sumNum}}件 合計金額：&yen;{{sumPrice|number_format(0, '.', ',')}}</p>
        {% if dataArr | length == 0 %}
          <p>保存している求人はありません</p>
        {% else %}
          {% for value in dataArr %}
            <div class="item">
              <ul>
                <li class="image">
                  <img src="{{constant('php\\Bootstrap::ENTRY_URL')}}images/{{value.image}}" alt="{{value.item_name}}">
                </li>
                <li class="name">{{value.item_name}}</li>
                <li class="price"> 日当 &yen; {{value.price|number_format(0,'.',',')}}</li>
                <li><a href="{{constant('php\\Bootstrap::ENTRY_URL')}}cart.php?crt_id={{value.crt_id}}">削除</a></li>
              </ul>
            </div>
          {% endfor %}
        {% endif %}
        </div>
      </div>
    </div>

    {% include 'footer.html.twig' %}
  </body>
</html>
